The app is deployed on heroku at https://glittering-gold.herokuapp.com/

On Unix system simply run 
> ./mvnw.cmd clean install 
or on Windows
> .mvnw.cmd clean install 
to to compile source.

The API is capped at around 100 calls per month so you may need to swap out the API key if the current one stops working.